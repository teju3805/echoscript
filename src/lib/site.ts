/**
 * Edit this once after you fork/push the repo — it is the only place the
 * GitHub link is defined. NEXT_PUBLIC_REPO_URL overrides it at build time.
 */
export const REPO_URL =
  process.env.NEXT_PUBLIC_REPO_URL || 'https://github.com/YOUR-GITHUB-USERNAME/echoscript';

export const APP_NAME = 'Echoscript';
